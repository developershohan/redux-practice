import React from 'react'
// import {useSelector} from "react-redux"
// import backgroundReducer from '../redux/backgroundReducer'

const Background = () => {
    // const background = useSelector((state) =>state)
  return (
    <div>
        
        <button >red</button>
    </div>
  )
}

export default Background